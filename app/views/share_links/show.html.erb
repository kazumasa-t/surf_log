<%# ========================================= %>
<%# 共有用サーフログ（月間・閲覧専用）       %>
<%# URL: /share/:token                        %>
<%# ========================================= %>

<% if @share_link.present? %>

  <%# ------------------------------ %>
  <%# ユーザー名表示                 %>
  <%# ------------------------------ %>
  <div style="font-weight:700; font-size:16px; margin-bottom:12px;">
    <%= @user.name %> さんのサーフログ
  </div>

  <%# ------------------------------ %>
  <%# カレンダー（共通パーシャル）   %>
  <%# editable: false → 閲覧専用      %>
  <%# ------------------------------ %>
  <%= render "surf_sessions/calendar",
      base_date: @base_date,
      total_hours: @total_hours,
      total_days: @total_days,
      streak_days: 0,
      sessions_by_date: @sessions_by_date,
      editable: false
  %>

<% else %>

  <p>この共有リンクは存在しないか、無効になっています。</p>

<% end %>
